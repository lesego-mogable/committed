services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: commit2linkedin
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: commit2linkedin
    ports:
      - "5434:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
